# IA 80/20 en Dashboard Bloqbase Implementation Plan

> **Para trabajadores agénticos:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) o superpowers:executing-plans para implementar este plan paso a paso. Los pasos usan checkbox (`- [ ]`) para tracking.

**Goal:** Agregar diagnóstico contextual y recomendaciones prioritarias (IA 80/20) en módulos Marketing y Ventas. La IA analiza datos, detecta el cuello de botella principal y muestra 1-3 acciones concretas con botones ejecutables.

**Architecture:** 
- Capa de análisis: funciones que reciben datos crudos, aplican reglas simples (comparaciones, baselines), y retornan un objeto de diagnóstico
- Componente visual: `AIRecommendation.tsx` que renderiza el diagnóstico, recomendación y botones de acción
- Integración: cada módulo llamará a su función de análisis dentro de `buildSnapshot()` y lo pasará a la página
- Sin API externa: toda la IA corre server-side en los `buildSnapshot()`, usa datos locales

**Tech Stack:**
- Next.js Server Actions para análisis y ejecución de acciones
- TypeScript strict para tipos de diagnóstico
- Tests unitarios con Vitest
- Componentes React para renderizar recomendaciones

---

## File Structure (Decomposition)

```
src/
├── core/
│   ├── components/
│   │   └── AIRecommendation.tsx          [NEW] Renderiza diagnóstico + recomendaciones + botones
│   ├── lib/
│   │   └── ai-engine.ts                  [NEW] Motor de análisis genérico (baselines, comparativas, detectar anomalías)
│   └── types/
│       └── ai.ts                         [NEW] Tipos comunes (Diagnosis, Recommendation, Action)
├── modules/
│   ├── marketing/
│   │   ├── lib/
│   │   │   ├── queries.ts                [MODIFY] Integrar análisis de Marketing en buildMarketingSnapshot()
│   │   │   ├── ai-analyzer.ts            [NEW] Lógica específica de análisis de Marketing
│   │   │   └── actions.ts                [NEW] Server actions para ejecutar recomendaciones
│   │   ├── components/
│   │   │   └── MarketingAIRecommendation.tsx [NEW] Componente especializado de Marketing
│   │   └── __tests__/
│   │       └── ai-analyzer.test.ts       [NEW]
│   └── ventas/
│       ├── lib/
│       │   ├── queries.ts                [MODIFY] Integrar análisis de Ventas en buildLlamadasSnapshot()
│       │   ├── ai-analyzer.ts            [NEW] Lógica específica de análisis de Ventas
│       │   └── actions.ts                [NEW] Server actions para ejecutar recomendaciones
│       ├── components/
│       │   └── VentasAIRecommendation.tsx [NEW] Componente especializado de Ventas
│       └── __tests__/
│           └── ai-analyzer.test.ts       [NEW]
└── app/
    ├── marketing/page.tsx                [MODIFY] Renderizar AIRecommendation con datos de Marketing
    └── ventas/page.tsx                   [MODIFY] Renderizar AIRecommendation con datos de Ventas
```

---

## Task 1: Create AI Core Types and Engine

### Files
- Create: `src/core/types/ai.ts`
- Create: `src/core/lib/ai-engine.ts`
- Test: `src/core/lib/__tests__/ai-engine.test.ts`

### Task 1.1: Write AI type definitions

- [ ] **Step 1: Create AI types file**

```typescript
// src/core/types/ai.ts
export type DiagnosisStatus = "bien" | "atención" | "crítico";

export interface Diagnosis {
  status: DiagnosisStatus;
  headline: string; // Una frase corta resumiendo el estado
  reason: string; // Por qué está así (datos que sustentan)
  context: Record<string, unknown>; // Datos contextuales para la recomendación
}

export interface Recommendation {
  title: string; // Acción específica y concreta
  description: string; // Una o dos frases explicando por qué
  priority: "alta" | "media" | "baja";
  metrics?: {
    label: string;
    current: string | number;
    expected?: string | number;
    delta?: string;
  }[]; // Métricas que sustentan la recomendación
}

export interface Action {
  id: string; // "marketing-increase-seo-budget", "ventas-reactivate-cold"
  label: string; // "Ejecutar"
  description?: string; // Hint sobre qué hace
  disabled?: boolean;
  isPending?: boolean;
}

export interface AIRecommendationData {
  diagnosis: Diagnosis;
  recommendations: Recommendation[]; // Máximo 3
  actions: Action[];
}
```

- [ ] **Step 2: Create AI analysis engine**

```typescript
// src/core/lib/ai-engine.ts

export interface BaselineData {
  current: number;
  previous: number;
  baseline: number;
}

export function detectTrend(baseline: BaselineData): {
  status: "up" | "down" | "stable";
  delta: number;
  deltaPercent: number;
} {
  const deltaPercent = ((baseline.current - baseline.previous) / baseline.previous) * 100;
  const delta = baseline.current - baseline.previous;

  if (Math.abs(deltaPercent) < 5) {
    return { status: "stable", delta, deltaPercent };
  }

  return {
    status: deltaPercent > 0 ? "up" : "down",
    delta,
    deltaPercent: Math.abs(deltaPercent),
  };
}

export function compareToBaseline(current: number, baseline: number): {
  isAboveBaseline: boolean;
  percent: number;
} {
  if (baseline === 0) return { isAboveBaseline: current > 0, percent: 0 };
  const percent = ((current - baseline) / baseline) * 100;
  return { isAboveBaseline: percent > 0, percent: Math.abs(percent) };
}

export function findBottleneck(metrics: Record<string, number>): {
  metricKey: string;
  value: number;
  impact: "alta" | "media" | "baja";
} | null {
  // Encuentra la métrica con peor ratio o mayor deterioro
  const entries = Object.entries(metrics).sort((a, b) => a[1] - b[1]);
  if (entries.length === 0) return null;

  const [key, value] = entries[0];
  const avgValue = Object.values(metrics).reduce((a, b) => a + b, 0) / Object.keys(metrics).length;
  const impact = value < avgValue * 0.8 ? "alta" : value < avgValue * 0.95 ? "media" : "baja";

  return { metricKey: key, value, impact };
}

export function generateDiagnosisStatus(signals: {
  mainMetric: BaselineData;
  bottleneck?: { impact: string };
  anomalies?: number;
}): import("@/core/types/ai").DiagnosisStatus {
  const trend = detectTrend(signals.mainMetric);

  if (trend.status === "down" && trend.deltaPercent > 20) {
    return "crítico";
  }

  if (signals.bottleneck?.impact === "alta") {
    return "atención";
  }

  if ((signals.anomalies ?? 0) > 2) {
    return "atención";
  }

  return "bien";
}
```

- [ ] **Step 3: Write test for AI engine**

```typescript
// src/core/lib/__tests__/ai-engine.test.ts
import { detectTrend, compareToBaseline, findBottleneck } from "../ai-engine";
import { describe, it, expect } from "vitest";

describe("ai-engine", () => {
  describe("detectTrend", () => {
    it("detects upward trend", () => {
      const result = detectTrend({ current: 110, previous: 100, baseline: 100 });
      expect(result.status).toBe("up");
      expect(result.deltaPercent).toBe(10);
    });

    it("detects downward trend", () => {
      const result = detectTrend({ current: 80, previous: 100, baseline: 100 });
      expect(result.status).toBe("down");
      expect(result.deltaPercent).toBe(20);
    });

    it("detects stable trend (< 5% change)", () => {
      const result = detectTrend({ current: 102, previous: 100, baseline: 100 });
      expect(result.status).toBe("stable");
    });
  });

  describe("compareToBaseline", () => {
    it("compares current to baseline", () => {
      const result = compareToBaseline(120, 100);
      expect(result.isAboveBaseline).toBe(true);
      expect(result.percent).toBe(20);
    });

    it("handles zero baseline", () => {
      const result = compareToBaseline(1, 0);
      expect(result.isAboveBaseline).toBe(true);
      expect(result.percent).toBe(0);
    });
  });

  describe("findBottleneck", () => {
    it("finds the lowest metric", () => {
      const result = findBottleneck({ clicks: 100, leads: 20, conversions: 10 });
      expect(result?.metricKey).toBe("conversions");
      expect(result?.value).toBe(10);
    });

    it("returns null for empty metrics", () => {
      const result = findBottleneck({});
      expect(result).toBeNull();
    });
  });
});
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
cd src
npm run test -- core/lib/__tests__/ai-engine.test.ts
```

Expected: All 6 tests pass

- [ ] **Step 5: Commit**

```bash
git add src/core/types/ai.ts src/core/lib/ai-engine.ts src/core/lib/__tests__/ai-engine.test.ts
git commit -m "feat(core): add AI engine with baseline detection and bottleneck analysis"
```

---

## Task 2: Create AIRecommendation Component

### Files
- Create: `src/core/components/AIRecommendation.tsx`
- Create: `src/core/components/__tests__/AIRecommendation.test.tsx`

### Task 2.1: Write AIRecommendation component

- [ ] **Step 1: Create component**

```typescript
// src/core/components/AIRecommendation.tsx
import { Badge } from "./Badge";
import type { AIRecommendationData } from "@/core/types/ai";

interface AIRecommendationProps {
  data: AIRecommendationData;
  onAction?: (actionId: string) => Promise<void>;
}

export function AIRecommendation({ data, onAction }: AIRecommendationProps) {
  const statusColors = {
    bien: "bg-green-50 border-green-200",
    atención: "border-yellow-200 bg-yellow-50",
    crítico: "border-red-200 bg-red-50",
  };

  const statusBadgeStatus = {
    bien: "hecho" as const,
    atención: "en_curso" as const,
    crítico: "bloqueado" as const,
  };

  return (
    <div
      className={`mt-[24px] border-l-4 p-[16px] rounded-[6px] ${statusColors[data.diagnosis.status]}`}
    >
      {/* Header */}
      <div className="flex items-start justify-between gap-[12px]">
        <div>
          <Badge status={statusBadgeStatus[data.diagnosis.status]}>
            {data.diagnosis.status.charAt(0).toUpperCase() + data.diagnosis.status.slice(1)}
          </Badge>
          <div className="bq-body-sm mt-[8px] font-semibold text-[rgba(26,26,24,0.8)]">
            {data.diagnosis.headline}
          </div>
        </div>
      </div>

      {/* Diagnosis reason */}
      <div className="mt-[10px] text-[13px] leading-[1.5] text-[rgba(26,26,24,0.6)]">
        {data.diagnosis.reason}
      </div>

      {/* Metrics if present */}
      {data.recommendations.length > 0 && data.recommendations[0].metrics && (
        <div className="mt-[12px] flex gap-[16px]">
          {data.recommendations[0].metrics.map((m, i) => (
            <div key={i} className="text-[12px]">
              <span className="block text-[rgba(26,26,24,0.55)]">{m.label}</span>
              <span className="block font-semibold text-[rgba(26,26,24,0.8)]">
                {m.current}
                {m.delta && <span className="text-[11px]"> {m.delta}</span>}
              </span>
            </div>
          ))}
        </div>
      )}

      {/* Recommendations */}
      {data.recommendations.length > 0 && (
        <div className="mt-[14px] space-y-[8px]">
          {data.recommendations.map((rec, i) => (
            <div key={i} className="text-[13px]">
              <div className="font-semibold text-[rgba(26,26,24,0.75)]">{rec.title}</div>
              <div className="mt-[2px] text-[rgba(26,26,24,0.6)]">{rec.description}</div>
            </div>
          ))}
        </div>
      )}

      {/* Action buttons */}
      {data.actions.length > 0 && (
        <div className="mt-[14px] flex gap-[8px]">
          {data.actions.map((action) => (
            <button
              key={action.id}
              disabled={action.disabled || action.isPending}
              onClick={() => onAction?.(action.id)}
              className="inline-flex items-center gap-[6px] rounded px-[12px] py-[6px] text-[12px] font-medium bg-[rgba(26,26,24,0.05)] text-[rgba(26,26,24,0.8)] hover:bg-[rgba(26,26,24,0.1)] disabled:opacity-50 disabled:cursor-not-allowed"
            >
              {action.isPending && <span className="inline-block h-[12px] w-[12px] animate-spin rounded-full border-2 border-current border-t-transparent" />}
              {action.label}
            </button>
          ))}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Write component test**

```typescript
// src/core/components/__tests__/AIRecommendation.test.tsx
import { render, screen } from "@testing-library/react";
import { AIRecommendation } from "../AIRecommendation";
import type { AIRecommendationData } from "@/core/types/ai";
import { describe, it, expect } from "vitest";

describe("AIRecommendation", () => {
  const mockData: AIRecommendationData = {
    diagnosis: {
      status: "atención",
      headline: "Caída en alcance de Instagram",
      reason: "El rendimiento de los últimos posts está 42% por debajo de la media histórica.",
      context: {},
    },
    recommendations: [
      {
        title: "Cambiar temática y formato",
        description: "Los posts con formato carrusel están rindiendo 3x mejor que videos.",
        priority: "alta",
      },
    ],
    actions: [
      {
        id: "prepare-instagram-posts",
        label: "Preparar posts",
      },
    ],
  };

  it("renders diagnosis status", () => {
    render(<AIRecommendation data={mockData} />);
    expect(screen.getByText(/Atención/)).toBeInTheDocument();
  });

  it("renders headline and reason", () => {
    render(<AIRecommendation data={mockData} />);
    expect(screen.getByText(/Caída en alcance de Instagram/)).toBeInTheDocument();
    expect(screen.getByText(/42%/)).toBeInTheDocument();
  });

  it("renders action buttons", () => {
    render(<AIRecommendation data={mockData} />);
    expect(screen.getByText(/Preparar posts/)).toBeInTheDocument();
  });
});
```

- [ ] **Step 3: Run component tests**

```bash
npm run test -- core/components/__tests__/AIRecommendation.test.tsx
```

Expected: All tests pass

- [ ] **Step 4: Commit**

```bash
git add src/core/components/AIRecommendation.tsx src/core/components/__tests__/AIRecommendation.test.tsx
git commit -m "feat(core): add AIRecommendation component"
```

---

## Task 3: Implement Marketing AI Analyzer

### Files
- Create: `src/modules/marketing/lib/ai-analyzer.ts`
- Create: `src/modules/marketing/lib/__tests__/ai-analyzer.test.ts`
- Modify: `src/modules/marketing/lib/queries.ts`

### Task 3.1: Create Marketing AI analyzer

- [ ] **Step 1: Write failing test for Marketing analyzer**

```typescript
// src/modules/marketing/lib/__tests__/ai-analyzer.test.ts
import { analyzeMarketingData } from "../ai-analyzer";
import type { MarketingSnapshot } from "../types";
import { describe, it, expect } from "vitest";

describe("Marketing AI Analyzer", () => {
  const mockSnapshot: MarketingSnapshot = {
    clicks30d: 1200,
    impresiones30d: 8000,
    posicionMedia: 12.5,
    paginasPublicadas: 45,
    paginasTotal: 50,
    postsBorrador: 5,
    postsProgramados: 3,
    postsPublicados: 42,
    oportunidades: [
      {
        id: "1",
        tipo: "CTR Bajo",
        score: 85,
        estado: "PENDIENTE",
        detectadaPorIa: true,
      },
      {
        id: "2",
        tipo: "Posición débil",
        score: 72,
        estado: "PENDIENTE",
        detectadaPorIa: false,
      },
    ],
    redes: [
      { canal: "LinkedIn", posts: 8, alcance: 2000, impresiones: 5000, clicks: 150, interacciones: 420 },
      { canal: "Instagram", posts: 12, alcance: 800, impresiones: 2400, clicks: 45, interacciones: 180 },
    ],
    seriesRedes: [],
    formulariosIniciados30d: 120,
    formulariosCompletados30d: 24,
    fecha: new Date(),
  };

  it("detects high priority SEO opportunity", () => {
    const result = analyzeMarketingData(mockSnapshot);
    expect(result.diagnosis.status).toBe("atención");
    expect(result.recommendations.length).toBeGreaterThan(0);
  });

  it("identifies conversion funnel issue", () => {
    const result = analyzeMarketingData(mockSnapshot);
    const conversionRec = result.recommendations.find((r) =>
      r.title.toLowerCase().includes("conversión")
    );
    // Si el ratio de conversión es bajo, debe aparecer una recomendación
    if (mockSnapshot.formulariosIniciados30d > 0) {
      const conversionRate = mockSnapshot.formulariosCompletados30d / mockSnapshot.formulariosIniciados30d;
      if (conversionRate < 0.25) {
        expect(conversionRec).toBeDefined();
      }
    }
  });

  it("always has at least one recommendation", () => {
    const result = analyzeMarketingData(mockSnapshot);
    expect(result.recommendations.length).toBeGreaterThanOrEqual(1);
    expect(result.recommendations.length).toBeLessThanOrEqual(3);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
npm run test -- modules/marketing/lib/__tests__/ai-analyzer.test.ts
```

Expected: FAIL - Module not found "ai-analyzer"

- [ ] **Step 3: Write Marketing analyzer implementation**

```typescript
// src/modules/marketing/lib/ai-analyzer.ts
import type { MarketingSnapshot, MarketingOportunidad } from "./types";
import type { AIRecommendationData, Recommendation } from "@/core/types/ai";
import { detectTrend, compareToBaseline, findBottleneck } from "@/core/lib/ai-engine";

export function analyzeMarketingData(snapshot: MarketingSnapshot): AIRecommendationData {
  const recommendations: Recommendation[] = [];

  // Señal 1: Conversión de formularios
  const conversionRate =
    snapshot.formulariosIniciados30d > 0
      ? snapshot.formulariosCompletados30d / snapshot.formulariosIniciados30d
      : 0;

  // Señal 2: Oportunidades pendientes con alto score
  const highScoreOpportunities = snapshot.oportunidades
    .filter((o) => o.estado === "PENDIENTE" && o.score != null)
    .sort((a, b) => (b.score ?? 0) - (a.score ?? 0));

  // Señal 3: Cobertura de páginas
  const coveragePercent = (snapshot.paginasPublicadas / snapshot.paginasTotal) * 100;

  // Determinar diagnóstico
  let status: "bien" | "atención" | "crítico" = "bien";
  let headline = "Marketing en buen estado";
  let reason = "Las métricas principales están dentro de lo esperado.";

  // Regla 1: Si hay muchas oportunidades SEO pendientes de alto score
  if (highScoreOpportunities.length > 5) {
    status = "atención";
    headline = `${highScoreOpportunities.length} oportunidades SEO esperando acción`;
    reason = `${highScoreOpportunities[0]?.tipo} está siendo la más prioritaria (score ${highScoreOpportunities[0]?.score}).`;

    recommendations.push({
      title: `Priorizar la oportunidad SEO #${highScoreOpportunities[0]?.id}`,
      description: `${highScoreOpportunities[0]?.tipo} tiene score ${highScoreOpportunities[0]?.score}. Actuar sobre esta puede traer clicks rápidamente.`,
      priority: "alta",
      metrics: [
        {
          label: "Oportunidades pendientes",
          current: highScoreOpportunities.length,
        },
      ],
    });
  }

  // Regla 2: Si la conversión de formularios es baja
  if (conversionRate > 0 && conversionRate < 0.2) {
    status = "atención";
    headline = "Conversion de formularios baja";
    reason = `Solo ${(conversionRate * 100).toFixed(1)}% de los inicios se convierten en leads. El embudo pierde ${snapshot.formulariosIniciados30d - snapshot.formulariosCompletados30d} leads potenciales.`;

    recommendations.push({
      title: "Optimizar el CTA y el paso de formulario",
      description:
        "Simplificar el formulario (máximo 3 campos) o cambiar el CTA a algo más urgente.",
      priority: "alta",
      metrics: [
        {
          label: "Tasa conversión actual",
          current: `${(conversionRate * 100).toFixed(1)}%`,
          expected: "25%",
        },
      ],
    });
  }

  // Regla 3: Si la cobertura está baja (menos del 80%)
  if (coveragePercent < 80) {
    status = status === "bien" ? "atención" : status;
    recommendations.push({
      title: "Publicar las páginas en borrador",
      description: `Tienes ${snapshot.paginasTotal - snapshot.paginasPublicadas} páginas sin publicar. Publicarlas amplía la cobertura SEO.`,
      priority: "media",
      metrics: [
        {
          label: "Cobertura",
          current: `${coveragePercent.toFixed(0)}%`,
          expected: "100%",
        },
      ],
    });
  }

  // Regla 4: Si no hay recomendaciones aún, agregar una genérica
  if (recommendations.length === 0) {
    recommendations.push({
      title: "Mantener el ritmo actual",
      description: "Las métricas principales están saludables. Continúa con la estrategia actual.",
      priority: "baja",
    });
  }

  // Limitar a 3 recomendaciones (máximo)
  const topRecommendations = recommendations.slice(0, 3);

  return {
    diagnosis: {
      status,
      headline,
      reason,
      context: {
        opportunitiesCount: highScoreOpportunities.length,
        conversionRate,
        coverage: coveragePercent,
      },
    },
    recommendations: topRecommendations,
    actions: [
      {
        id: "marketing-review-opportunities",
        label: "Ver oportunidades",
      },
      {
        id: "marketing-prepare-content",
        label: "Preparar contenido",
      },
    ],
  };
}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
npm run test -- modules/marketing/lib/__tests__/ai-analyzer.test.ts
```

Expected: All 3 tests pass

- [ ] **Step 5: Integrate analyzer into buildMarketingSnapshot**

Modify `src/modules/marketing/lib/queries.ts` at the end of the function:

```typescript
// At the end of buildMarketingSnapshot(), before the return statement
export async function buildMarketingSnapshot(sql?: Sql): Promise<MarketingSnapshot> {
  // ... existing code ...

  const snapshot: MarketingSnapshot = {
    // ... existing fields ...
  };

  // Agregar el análisis de IA (será usado por la página)
  (snapshot as any).aiAnalysis = analyzeMarketingData(snapshot);

  return snapshot;
}
```

Also add to `types.ts`:

```typescript
// In src/modules/marketing/lib/types.ts, add to MarketingSnapshot interface
export interface MarketingSnapshot {
  // ... existing fields ...
  aiAnalysis?: AIRecommendationData;
}
```

- [ ] **Step 6: Commit**

```bash
git add src/modules/marketing/lib/ai-analyzer.ts src/modules/marketing/lib/__tests__/ai-analyzer.test.ts src/modules/marketing/lib/queries.ts src/modules/marketing/lib/types.ts
git commit -m "feat(marketing): add AI analyzer with opportunity prioritization"
```

---

## Task 4: Implement Ventas AI Analyzer

### Files
- Create: `src/modules/ventas/lib/ai-analyzer.ts`
- Create: `src/modules/ventas/lib/__tests__/ai-analyzer.test.ts`
- Modify: `src/modules/ventas/lib/queries.ts`
- Modify: `src/modules/ventas/lib/types.ts`

### Task 4.1: Create Ventas AI analyzer

- [ ] **Step 1: Write failing test for Ventas analyzer**

```typescript
// src/modules/ventas/lib/__tests__/ai-analyzer.test.ts
import { analyzeLlamadasData } from "../ai-analyzer";
import type { VentasSnapshot } from "../types";
import { describe, it, expect } from "vitest";

describe("Ventas AI Analyzer", () => {
  const mockSnapshot: Partial<VentasSnapshot> = {
    llamadas7d: 8,
    llamadasPositivas7d: 2,
    llamadasRecientes: [
      {
        id: "1",
        prospecto: "Empresa A",
        resultado: "INTERESADO",
        resumen: "Buena conversación, interés en demostración",
      },
      {
        id: "2",
        prospecto: "Empresa B",
        resultado: "NO_INTERESADO",
        resumen: "Sin necesidad actual",
      },
    ],
  };

  it("detects low positive rate", () => {
    const result = analyzeLlamadasData(mockSnapshot as VentasSnapshot);
    expect(result.diagnosis.status).toBe("atención");
    // 2/8 = 25% de positivas es bajo
  });

  it("always returns max 3 recommendations", () => {
    const result = analyzeLlamadasData(mockSnapshot as VentasSnapshot);
    expect(result.recommendations.length).toBeLessThanOrEqual(3);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
npm run test -- modules/ventas/lib/__tests__/ai-analyzer.test.ts
```

Expected: FAIL

- [ ] **Step 3: Write Ventas analyzer implementation**

```typescript
// src/modules/ventas/lib/ai-analyzer.ts
import type { VentasSnapshot } from "./types";
import type { AIRecommendationData, Recommendation } from "@/core/types/ai";

export function analyzeLlamadasData(snapshot: VentasSnapshot): AIRecommendationData {
  const recommendations: Recommendation[] = [];

  const positiveRate =
    snapshot.llamadas7d > 0 ? snapshot.llamadasPositivas7d / snapshot.llamadas7d : 0;

  // Señal 1: Tasa de resultado positivo
  const lowPositiveRate = positiveRate < 0.35; // Menos del 35% es bajo

  // Señal 2: Volumen de llamadas
  const lowVolume = snapshot.llamadas7d < 5;

  // Señal 3: Análisis de llamadas sin resultado definido
  const sinResultado = snapshot.llamadasRecientes.filter(
    (l) => l.resultado === "SIN_RESULTADO"
  ).length;

  let status: "bien" | "atención" | "crítico" = "bien";
  let headline = "Pipeline de ventas saludable";
  let reason = "Está manteniendo el ritmo esperado de llamadas y conversiones.";

  // Regla 1: Baja tasa de positivas
  if (lowPositiveRate && snapshot.llamadas7d > 0) {
    status = "atención";
    headline = `Tasa de positivas baja: ${(positiveRate * 100).toFixed(0)}%`;
    reason = `De las ${snapshot.llamadas7d} llamadas, solo ${snapshot.llamadasPositivas7d} fueron positivas. El embudo tiene mucho rechazo temprano.`;

    recommendations.push({
      title: "Revisar el guión de presentación",
      description:
        "Si muchas llamadas terminan en NO_INTERESADO sin discusión real, el problema está en los primeros 60 segundos.",
      priority: "alta",
      metrics: [
        {
          label: "Tasa positiva",
          current: `${(positiveRate * 100).toFixed(0)}%`,
          expected: "35-50%",
        },
      ],
    });
  }

  // Regla 2: Bajo volumen
  if (lowVolume) {
    status = status === "bien" ? "atención" : status;
    headline = lowVolume ? `Solo ${snapshot.llamadas7d} llamadas esta semana` : headline;

    recommendations.push({
      title: "Aumentar volumen de llamadas",
      description:
        "Menos de 5 llamadas por semana es insuficiente para generar oportunidades. Necesitas al menos 10-15.",
      priority: "alta",
      metrics: [
        {
          label: "Llamadas (7d)",
          current: snapshot.llamadas7d,
          expected: "15",
        },
      ],
    });
  }

  // Regla 3: Llamadas sin resultado
  if (sinResultado > 0) {
    recommendations.push({
      title: `Clasificar ${sinResultado} llamada${sinResultado > 1 ? "s" : ""} sin resultado`,
      description:
        "Completar el registro de estas llamadas ayuda a entender mejor el pipeline.",
      priority: "media",
    });
  }

  // Regla 4: Genérica si todo está bien
  if (recommendations.length === 0) {
    recommendations.push({
      title: "Mantener ritmo actual",
      description: "Las métricas están dentro de lo esperado.",
      priority: "baja",
    });
  }

  return {
    diagnosis: {
      status,
      headline,
      reason,
      context: {
        positiveRate,
        volume: snapshot.llamadas7d,
        undefined: sinResultado,
      },
    },
    recommendations: recommendations.slice(0, 3),
    actions: [
      {
        id: "ventas-review-calls",
        label: "Ver llamadas",
      },
      {
        id: "ventas-reactivate-cold",
        label: "Reactivar leads fríos",
      },
    ],
  };
}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
npm run test -- modules/ventas/lib/__tests__/ai-analyzer.test.ts
```

Expected: All tests pass

- [ ] **Step 5: Modify VentasSnapshot type**

Add to `src/modules/ventas/lib/types.ts`:

```typescript
import type { AIRecommendationData } from "@/core/types/ai";

export interface VentasSnapshot {
  // ... existing fields ...
  aiAnalysis?: AIRecommendationData;
}
```

- [ ] **Step 6: Integrate analyzer into buildLlamadasSnapshot**

Modify `src/modules/ventas/lib/queries.ts`:

```typescript
// Import the analyzer
import { analyzeLlamadasData } from "./ai-analyzer";

// At the end of buildLlamadasSnapshot()
export async function buildLlamadasSnapshot(sql?: Sql): Promise<Omit<VentasSnapshot, "leadsNuevosSemana" | "pipelineProspeccion" | "fecha">> {
  // ... existing code ...

  const result = {
    llamadas7d: llamadasRecientes.length,
    llamadasPositivas7d: llamadasRecientes.filter((l) => l.resultado === "INTERESADO").length,
    llamadasRecientes,
  };

  // Create a temporary snapshot for analysis
  const tempSnapshot: VentasSnapshot = {
    ...result,
    leadsNuevosSemana: 0,
    pipelineProspeccion: [],
    fecha: new Date(),
  };

  (result as any).aiAnalysis = analyzeLlamadasData(tempSnapshot);

  return result;
}
```

- [ ] **Step 7: Commit**

```bash
git add src/modules/ventas/lib/ai-analyzer.ts src/modules/ventas/lib/__tests__/ai-analyzer.test.ts src/modules/ventas/lib/queries.ts src/modules/ventas/lib/types.ts
git commit -m "feat(ventas): add AI analyzer with call quality detection"
```

---

## Task 5: Create Server Actions for Executing Recommendations

### Files
- Create: `src/modules/marketing/lib/actions.ts`
- Create: `src/modules/ventas/lib/actions.ts`

### Task 5.1: Marketing actions

- [ ] **Step 1: Create Marketing server actions**

```typescript
// src/modules/marketing/lib/actions.ts
"use server";

import { redirect } from "next/navigation";

export async function executeMarketingAction(actionId: string) {
  console.log(`[marketing] Executing action: ${actionId}`);

  switch (actionId) {
    case "marketing-review-opportunities":
      // En el futuro, esto podría abrir un modal o ir a una sección específica
      console.log("Redirecting to opportunities section");
      break;

    case "marketing-prepare-content":
      // En el futuro, esto podría integrar con un CMS o draft creator
      console.log("Preparing content workflow");
      break;

    default:
      console.warn(`[marketing] Unknown action: ${actionId}`);
  }

  // Por ahora, solo loguear. En el futuro:
  // - Crear tareas en Asana/Linear
  // - Enviar notificaciones
  // - Trigger workflows en n8n
}
```

- [ ] **Step 2: Create Ventas server actions**

```typescript
// src/modules/ventas/lib/actions.ts
"use server";

export async function executeVentasAction(actionId: string) {
  console.log(`[ventas] Executing action: ${actionId}`);

  switch (actionId) {
    case "ventas-review-calls":
      console.log("Reviewing recent calls");
      break;

    case "ventas-reactivate-cold":
      // En el futuro, preparar una lista de leads para reactivación
      console.log("Preparing reactivation workflow");
      break;

    default:
      console.warn(`[ventas] Unknown action: ${actionId}`);
  }
}
```

- [ ] **Step 3: Commit**

```bash
git add src/modules/marketing/lib/actions.ts src/modules/ventas/lib/actions.ts
git commit -m "feat: add server actions for executing recommendations"
```

---

## Task 6: Integrate AIRecommendation into Marketing Page

### Files
- Modify: `src/app/marketing/page.tsx`
- Create: `src/modules/marketing/components/MarketingAIRecommendation.tsx`

### Task 6.1: Update Marketing page

- [ ] **Step 1: Create specialized Marketing AI component**

```typescript
// src/modules/marketing/components/MarketingAIRecommendation.tsx
"use client";

import { AIRecommendation } from "@/core/components/AIRecommendation";
import { executeMarketingAction } from "@/modules/marketing/lib/actions";
import type { AIRecommendationData } from "@/core/types/ai";
import { useState } from "react";

interface MarketingAIRecommendationProps {
  data: AIRecommendationData;
}

export function MarketingAIRecommendation({ data }: MarketingAIRecommendationProps) {
  const [pending, setPending] = useState<string | null>(null);

  async function handleAction(actionId: string) {
    setPending(actionId);
    try {
      await executeMarketingAction(actionId);
    } catch (err) {
      console.error("Action failed:", err);
    } finally {
      setPending(null);
    }
  }

  return (
    <AIRecommendation
      data={{
        ...data,
        actions: data.actions.map((a) => ({
          ...a,
          isPending: pending === a.id,
        })),
      }}
      onAction={handleAction}
    />
  );
}
```

- [ ] **Step 2: Modify marketing page to render AI component**

```typescript
// Modify src/app/marketing/page.tsx
// Add import at the top
import { MarketingAIRecommendation } from "@/modules/marketing/components/MarketingAIRecommendation";

// In the component, after the "01 — TRÁFICO" section and before the opportunities table:
{snapshot?.aiAnalysis && (
  <MarketingAIRecommendation data={snapshot.aiAnalysis} />
)}
```

Full section after modification:

```typescript
{/* 01 — TRÁFICO */}
<section className="mt-[40px] border-t-2 border-[color:var(--grafito)] pt-[26px]">
  <div className="bq-sec-head">
    <span className="bq-sec-num">01</span>
    <span className="bq-sec-title">Tráfico</span>
  </div>
  <div className="mt-[8px] max-w-[480px] text-[13px] leading-[1.55] text-[rgba(26,26,24,0.55)]">
    Búsqueda orgánica y cobertura de páginas indexadas.
  </div>

  <div className="mt-[22px] grid grid-cols-1 gap-[16px] sm:grid-cols-2 lg:grid-cols-4">
    <MetricCard label="Clicks (30d)" value={snapshot ? String(snapshot.clicks30d) : "—"} />
    <MetricCard label="Impresiones (30d)" value={snapshot ? String(snapshot.impresiones30d) : "—"} />
    <MetricCard
      label="Posición media"
      value={snapshot?.posicionMedia != null ? String(snapshot.posicionMedia) : "—"}
    />
    <MetricCard
      label="Páginas publicadas"
      value={snapshot ? `${snapshot.paginasPublicadas}/${snapshot.paginasTotal}` : "—"}
    />
  </div>

  {/* IA RECOMMENDATION */}
  {snapshot?.aiAnalysis && (
    <MarketingAIRecommendation data={snapshot.aiAnalysis} />
  )}

  <div className="mt-[24px]">
    {oportunidades.length > 0 ? (
      // ... existing table code ...
    ) : (
      // ... existing empty state ...
    )}
  </div>
</section>
```

- [ ] **Step 3: Commit**

```bash
git add src/modules/marketing/components/MarketingAIRecommendation.tsx src/app/marketing/page.tsx
git commit -m "feat(marketing): integrate AI recommendations into page"
```

---

## Task 7: Integrate AIRecommendation into Ventas Page

### Files
- Modify: `src/app/ventas/page.tsx`
- Create: `src/modules/ventas/components/VentasAIRecommendation.tsx`

### Task 7.1: Update Ventas page

- [ ] **Step 1: Create specialized Ventas AI component**

```typescript
// src/modules/ventas/components/VentasAIRecommendation.tsx
"use client";

import { AIRecommendation } from "@/core/components/AIRecommendation";
import { executeVentasAction } from "@/modules/ventas/lib/actions";
import type { AIRecommendationData } from "@/core/types/ai";
import { useState } from "react";

interface VentasAIRecommendationProps {
  data: AIRecommendationData;
}

export function VentasAIRecommendation({ data }: VentasAIRecommendationProps) {
  const [pending, setPending] = useState<string | null>(null);

  async function handleAction(actionId: string) {
    setPending(actionId);
    try {
      await executeVentasAction(actionId);
    } catch (err) {
      console.error("Action failed:", err);
    } finally {
      setPending(null);
    }
  }

  return (
    <AIRecommendation
      data={{
        ...data,
        actions: data.actions.map((a) => ({
          ...a,
          isPending: pending === a.id,
        })),
      }}
      onAction={handleAction}
    />
  );
}
```

- [ ] **Step 2: Modify ventas page to render AI component**

Modify `src/app/ventas/page.tsx` - add import and update the metrics section:

```typescript
// Add import at the top
import { VentasAIRecommendation } from "@/modules/ventas/components/VentasAIRecommendation";

// In the JSX, after the metrics grid, before "Llamadas recientes" section:
{llamadasData?.aiAnalysis && (
  <VentasAIRecommendation data={llamadasData.aiAnalysis} />
)}
```

Full section after modification:

```typescript
<div className="mt-[32px] grid grid-cols-1 gap-[16px] sm:grid-cols-3">
  <MetricCard label="Llamadas (7d)" value={String(llamadas7d)} />
  <MetricCard
    label="Resultado positivo"
    value={llamadas7d > 0 ? `${Math.round((llamadasPositivas7d / llamadas7d) * 100)}%` : "—"}
  />
  <MetricCard label="Leads en pipeline" value={String(pipeline.length)} />
</div>

{/* IA RECOMMENDATION */}
{llamadasData?.aiAnalysis && (
  <VentasAIRecommendation data={llamadasData.aiAnalysis} />
)}

<section className="mt-[56px] border-t-2 border-[color:var(--grafito)] pt-[26px]">
  {/* ... rest of the section ... */}
</section>
```

- [ ] **Step 3: Run dev server to test visually**

```bash
npm run dev
```

Navigate to `/marketing` and `/ventas` to see the AI recommendations rendered.

- [ ] **Step 4: Commit**

```bash
git add src/modules/ventas/components/VentasAIRecommendation.tsx src/app/ventas/page.tsx
git commit -m "feat(ventas): integrate AI recommendations into page"
```

---

## Task 8: Update Resumen (Inicio) with simple IA status aggregation

### Files
- Modify: `src/app/page.tsx`
- Modify: `src/modules/resumen/lib/read.ts`

### Task 8.1: Aggregate status from submodules

- [ ] **Step 1: Modify resumen reader to include module status**

```typescript
// src/modules/resumen/lib/read.ts
// Add this function alongside readLatestSnapshots

export async function readModuleStatuses(): Promise<{
  marketing: "bien" | "atención" | "crítico";
  ventas: "bien" | "atención" | "crítico";
}> {
  let marketingStatus: "bien" | "atención" | "crítico" = "bien";
  let ventasStatus: "bien" | "atención" | "crítico" = "bien";

  try {
    const marketingSnap = await buildMarketingSnapshot();
    marketingStatus = marketingSnap.aiAnalysis?.diagnosis.status ?? "bien";
  } catch (err) {
    console.warn("[resumen] failed to read marketing status", err);
  }

  try {
    const ventasSnap = await buildLlamadasSnapshot();
    ventasStatus = ventasSnap.aiAnalysis?.diagnosis.status ?? "bien";
  } catch (err) {
    console.warn("[resumen] failed to read ventas status", err);
  }

  return { marketing: marketingStatus, ventas: ventasStatus };
}
```

- [ ] **Step 2: Update Resumen page to show status**

Modify `src/app/page.tsx`:

```typescript
// Add import
import { readModuleStatuses } from "@/modules/resumen/lib/read";

// Modify ResumenPage function:
export default async function ResumenPage() {
  const { marketing, ventas } = await readLatestSnapshots();
  const moduleStatuses = await readModuleStatuses();

  // ... rest of component ...
  // In the metrics grid, update the "Estado por módulo" display
  // You can create small status badges for each module
}
```

- [ ] **Step 3: Commit**

```bash
git add src/modules/resumen/lib/read.ts src/app/page.tsx
git commit -m "feat(resumen): add module status aggregation"
```

---

## Task 9: Run full test suite and verify no regressions

### Task 9.1: Test everything

- [ ] **Step 1: Run all tests**

```bash
npm run test
```

Expected: All existing tests pass, plus the new AI analyzer tests

- [ ] **Step 2: Build project**

```bash
npm run build
```

Expected: Build succeeds with no errors

- [ ] **Step 3: Do a manual smoke test**

Visit `/marketing` and `/ventas` in the dev server. Verify:
- AI recommendations appear below the metrics
- No console errors
- Buttons are clickable (they'll log for now)

- [ ] **Step 4: Commit if any fixes were needed**

```bash
git commit -m "chore: pass full test suite and build checks"
```

---

## Task 10: Final review and documentation

### Files
- Create: `docs/AI-ENGINE.md`
- Modify: `README.md` (if exists)

### Task 10.1: Document the IA system

- [ ] **Step 1: Create AI engine documentation**

```markdown
// docs/AI-ENGINE.md
# Dashboard AI 80/20 Engine

## Overview

El motor de IA del dashboard identifica automáticamente el cuello de botella principal en cada módulo (Marketing, Ventas) y propone 1-3 acciones prioritarias basadas en datos.

## Architecture

### Core Components

**AI Engine** (`src/core/lib/ai-engine.ts`)
- Funciones genéricas de análisis: `detectTrend()`, `compareToBaseline()`, `findBottleneck()`
- Usado por analizadores específicos de cada módulo

**Module Analyzers**
- `src/modules/marketing/lib/ai-analyzer.ts` — Detecta oportunidades SEO, tasa de conversión baja, etc.
- `src/modules/ventas/lib/ai-analyzer.ts` — Detecta baja tasa de positivas, bajo volumen, etc.

**AIRecommendation Component** (`src/core/components/AIRecommendation.tsx`)
- Renderiza diagnóstico, recomendaciones y botones de acción
- Estilo consistente en todos los módulos

### Data Flow

1. Página llama a `buildSnapshot()` (ej: `buildMarketingSnapshot()`)
2. `buildSnapshot()` recopila datos y llama a su analizador (ej: `analyzeMarketingData()`)
3. Analizador retorna `AIRecommendationData` con diagnóstico + recomendaciones
4. Página renderiza `<AIRecommendation data={snapshot.aiAnalysis} />`
5. Usuario hace click en botón → Server Action se ejecuta

## Adding New Analysis Rules

Para agregar una nueva regla de análisis en Marketing:

1. Abre `src/modules/marketing/lib/ai-analyzer.ts`
2. En `analyzeMarketingData()`, agrega una nueva "Regla N" antes del return
3. Ejemplo:

```typescript
// Regla 5: Si el CTR es muy bajo
const avgCTR = snapshot.clicks30d / snapshot.impresiones30d;
if (avgCTR < 0.01) { // Menos del 1%
  status = "atención";
  recommendations.push({
    title: "Mejorar los títulos y meta descriptions",
    description: "Un CTR bajo indica que los usuarios no ven tus resultados como relevantes.",
    priority: "alta",
  });
}
```

4. Corre tests: `npm run test -- modules/marketing/lib/__tests__/ai-analyzer.test.ts`
5. Commit: `git commit -m "feat(marketing): add rule for low CTR detection"`

## Testing

Cada analizador tiene tests unitarios en `__tests__/ai-analyzer.test.ts`. Corren con:

```bash
npm run test -- modules/*/lib/__tests__/ai-analyzer.test.ts
```

Tests siguen patrón arrange-act-assert y usan mocks de datos simples.

## Future Enhancements

- [ ] Integración con Claude API para recomendaciones con lenguaje natural
- [ ] Históricos y tendencias para baselines dinámicos
- [ ] Webhooks para ejecutar acciones automáticamente en n8n
- [ ] Alerts configurables (email, Slack, SMS)
- [ ] A/B testing de recomendaciones (cuál genera más engagement)
```

- [ ] **Step 2: Commit**

```bash
git add docs/AI-ENGINE.md
git commit -m "docs: add AI engine documentation"
```

---

## Summary

**Commits created:**
1. `feat(core): add AI engine with baseline detection and bottleneck analysis`
2. `feat(core): add AIRecommendation component`
3. `feat(marketing): add AI analyzer with opportunity prioritization`
4. `feat(ventas): add AI analyzer with call quality detection`
5. `feat: add server actions for executing recommendations`
6. `feat(marketing): integrate AI recommendations into page`
7. `feat(ventas): integrate AI recommendations into page`
8. `feat(resumen): add module status aggregation`
9. `chore: pass full test suite and build checks` (if needed)
10. `docs: add AI engine documentation`

---

## Execution Options

**Plan complete and saved to `docs/superpowers/plans/2026-09-23-ia-80-20-mvp.md`.**

Two execution approaches available:

### 1. **Subagent-Driven (recommended)**
- I dispatch a fresh subagent per task
- Review between tasks
- Faster iteration, better error handling

### 2. **Inline Execution**
- Execute tasks in this session
- Batch execution with checkpoints for review

**Which approach do you prefer?**
